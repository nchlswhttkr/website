version: "3"
services:
    ci:
        working_dir: /workdir
        volumes:
            - ..:/workdir
        build:
            context: .
            args:
                HUGO_VERSION: "0.104.3"
    backup:
        working_dir: /workdir
        volumes:
            - ..:/workdir
        image: "python:3.10"
